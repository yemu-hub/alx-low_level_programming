#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: first input parameter.
 * @b: second input parameter.
 * Return: resopond product.
 */

int mul(int a, int b)
{
	return(a*b);
}
